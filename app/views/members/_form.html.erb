<%= form_for @member, url: form_url, html: { method: form_method } do |f| %>
  <%= f.label :email %>
  <%= f.text_field :email %>

  <%= f.label :full_name %>
  <%= f.text_field :full_name %>

  <%= f.submit submit_button_label %>
<% end %>