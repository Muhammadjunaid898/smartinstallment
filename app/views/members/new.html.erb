<h1>Create Members</h1>
<%= form_for @member, url: members_path do |f| %>
	<%= f.label :email %>
	<%= f.text_field :email %>

	<%= f.label :full_name %>
	<%= f.text_field :full_name %>

	<%= f.submit "Create User" %>
<% end %>